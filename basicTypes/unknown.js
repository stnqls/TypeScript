"use strict";
// const aNumber: number = maybe; err
if (maybe === true) {
    const aBoolean = maybe;
}
if (typeof maybe === "string") {
    const aString = maybe;
}
if (typeof maybe === "number") {
    const aNumber = maybe;
}
